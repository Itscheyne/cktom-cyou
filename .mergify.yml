pull_request_rules:
  - name: Automatic merge on approval and successful CI
    conditions:
      - check-success=lint-branch
      - check-success=tofu validate plan
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: squash
